<h2>組織への招待</h2>

<p>
  <strong><%= @inviter.name.presence || @inviter.email %></strong> さんから、
  <strong><%= @organization.name %></strong> への招待が届いています
</p>

<p>あなたは <strong><%= @role_name %></strong> として招待されています</p>

<p>以下のボタンをクリックして招待を承認してください：</p>

<div style="text-align: center; margin: 32px 0;">
  <a href="<%= @accept_url %>"
     style="background-color: #4F46E5; color: #ffffff; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: bold; display: inline-block;">
    招待を承認する
  </a>
</div>

<p style="font-size: 12px; color: #6b7280; margin-top: 24px; border-top: 1px solid #e5e7eb; padding-top: 12px;">
  ボタンが機能しない場合は、以下のURLをコピーしてブラウザに貼り付けてください：<br>
  <a href="<%= @accept_url %>" style="color: #4F46E5;"><%= @accept_url %></a>
</p>

<% if @invitation.expires_at.present? %>
  <p style="font-size: 12px; color: #dc2626; margin-top: 16px;">
    ※ この招待は <%= l(@invitation.expires_at.in_time_zone('Tokyo'), format: :long) %> まで有効です
  </p>
<% end %>
